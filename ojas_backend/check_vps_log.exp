#!/usr/bin/expect -f
set timeout 10
spawn ssh root@72.61.172.182 "tail -n 20 /var/log/ojas_backend.log"
expect {
    "Are you sure you want to continue connecting" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "R@dhe7906342450\r"
    }
}
expect eof
