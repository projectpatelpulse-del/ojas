#!/usr/bin/expect -f
set timeout 30
spawn ssh root@72.61.172.182 "ls -R /root /root/backend /home /home/backend 2>/dev/null | grep -E ':|server.js|userController.js'"
expect {
    "Are you sure you want to continue connecting" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "R@dhe7906342450\r"
    }
}
expect eof
