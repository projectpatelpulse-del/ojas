#!/usr/bin/expect -f
set timeout 30
spawn ssh root@72.61.172.182 "ps aux | grep -E 'node|pm2' | grep -v grep"
expect {
    "Are you sure you want to continue connecting" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "R@dhe7906342450\r"
    }
}
expect eof
